# Linux Kernel Observer

A terminal tool that observes low-level Linux kernel activity using:

- /proc interrupts
- system load
- per-core CPU usage
- active processes

This project explores how the Linux kernel exposes runtime system data.
